# Toast 宽度自适应 — 已完成

## 状态：已完成

`src/components/Toast/Toast.module.css` 第 15 行已设置为 `max-width: 92vw`。

如果手机上仍然看到溢出，请尝试：
1. 强制刷新浏览器（Ctrl+Shift+R 或 Cmd+Shift+R）
2. 清除浏览器缓存后刷新
3. 重启开发服务器（停止 `npm run dev` 后重新运行）
