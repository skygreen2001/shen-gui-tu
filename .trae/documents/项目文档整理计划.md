# 「申归途」项目文档整理计划

## 概述

当前项目文档存在以下问题需要整理：
1. `shen-gui-tu/README.md` 仍为 Vite 脚手架默认内容，需要替换为项目专属 README
2. 根目录 workspace 下散落着 9 个 `prototype-*.html` 原型文件，应移入 `shen-gui-tu/docs/prototype/`
3. `shen-gui-tu/src/` 下存在 Vite 默认生成的冗余文件（`App.css`、`index.css`、`assets/` 中的 Vite/React logo），应清理
4. `.trae/documents/` 与 `shen-gui-tu/docs/` 存在内容重叠，需明确各自定位

## 当前状态分析

### 文档分布
| 位置 | 文件 | 状态 |
|------|------|------|
| `shen-gui-tu/docs/` | 3份核心文档 + 8张图表 + 9个原型 | ✅ 已整理 |
| `.trae/documents/` | 3份设计/计划文档 | ⚠️ 开发上下文，保留 |
| `workspace/根目录` | 9个 prototype-*.html | ❌ 需移入 docs |
| `shen-gui-tu/README.md` | Vite 默认模板 | ❌ 需替换 |

### 冗余文件
| 文件 | 说明 |
|------|------|
| `shen-gui-tu/src/App.css` | Vite 默认生成，项目未使用（使用 CSS Modules） |
| `shen-gui-tu/src/index.css` | Vite 默认生成，项目未使用（使用 styles/global.css） |
| `shen-gui-tu/src/assets/react.svg` | Vite 默认 logo，未使用 |
| `shen-gui-tu/src/assets/vite.svg` | Vite 默认 logo，未使用 |
| `shen-gui-tu/public/vite.svg` | Vite 默认 favicon，未使用 |

## 整理步骤

### 步骤 1：清理根目录散落的原型文件
- 将 `workspace/prototype-01-global.html` 至 `workspace/prototype-09-integrated.html`（共9个文件）移入 `shen-gui-tu/docs/prototype/`
- 如果目标位置已有同名文件则跳过（避免覆盖）

### 步骤 2：清理 src/ 冗余文件
- 删除 `shen-gui-tu/src/App.css`
- 删除 `shen-gui-tu/src/index.css`
- 删除 `shen-gui-tu/src/assets/react.svg`
- 删除 `shen-gui-tu/src/assets/vite.svg`
- 删除 `shen-gui-tu/public/vite.svg`

### 步骤 3：编写项目 README.md
替换 `shen-gui-tu/README.md` 为项目专属内容，包含：
- 项目名称与简介（「申归途」— 上海抑郁症复发预防支持工具）
- 项目背景（参赛 SOLO 挑战赛公益赛道）
- 核心功能（8个模块简介）
- 技术栈（React 18 + Vite + HashRouter + CSS Modules + LocalStorage）
- 项目结构（目录树）
- 快速开始（安装/开发/构建命令）
- 设计文档索引（指向 docs/ 目录）
- 许可证

### 步骤 4：验证构建
- 运行 `npx vite build` 确认清理后项目仍可正常构建
- 确认无引用断裂

## 假设与决策
- `.trae/documents/` 下的文档保留不动（它们是 TRAE IDE 的开发上下文）
- `docs/` 定位为项目交付文档（调研报告、设计方案、原型）
- README.md 使用中文编写（与项目语言一致）
