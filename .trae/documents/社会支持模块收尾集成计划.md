# 社会支持与生活重建模块 — 收尾集成计划

## 概要

上一轮会话已完成方案设计（已获批）和 4 个新文件的创建，本计划仅覆盖剩余的集成与验证工作。

## 当前状态

| 文件 | 状态 |
|------|------|
| `src/data/socialTasks.js` | ✅ 已创建（5 阶段 20 任务） |
| `src/data/peerStories.js` | ✅ 已创建（8 篇故事 + 分类） |
| `src/pages/Resources/RebuildTab.jsx` | ✅ 已创建（完整组件） |
| `src/pages/Resources/RebuildTab.module.css` | ✅ 已创建（完整样式） |
| `src/pages/Resources/Resources.jsx` | ❌ 需修改（集成第 6 个 tab） |

## 实施步骤

### Step 1：修改 Resources.jsx

文件：`src/pages/Resources/Resources.jsx`

改动点：
1. **新增 import**：`import RebuildTab from './RebuildTab'`
2. **tabs 数组追加第 6 项**：`{ key: 'rebuild', label: '🌱 重建' }`
3. **新增渲染分支**：`{activeTab === 5 && <RebuildTab />}`

### Step 2：构建验证

运行 `npx vite build`，确认 0 错误。

## 验证

- `npx vite build` 输出 0 errors
- 启动 `npx vite preview`，浏览器打开服务中心页面，确认第 6 个 tab "🌱 重建" 可见且可点击
